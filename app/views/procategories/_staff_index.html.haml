.post
  %h2.title
    = t("views.procategory.procategories_list")
  %h3.posted
    = format_date(DateTime.now)
  .story
    %p
      %img{:alt => "", :height => "140", :src => "../assets/procategories.jpg", :width => "470"}/
    %p
      %strong
        = t("views.procategory.procategories_list")
      = t("views.procategory.procategories_description", now: format_date(DateTime.now))
  .meta
    %p
      = t("views.procategory.procategories_count")+': '
      = @procategories.count

%table.projects-table
  %thead
    %tr
      %th.number
        = t("views.number")
      %th.body
        = t("activerecord.models.procategory")
  %tbody
    - if params[:page] && Integer(params[:page]) > 1
      - flag = (Integer(params[:page]) - 1) *20 + 1
    - else
      - flag = 1
    - @procategories.each do |procategory|
      %tr{class: cycle('list_line_odd', 'list_line_even')}
        %td.number
          = flag
          - flag+= 1
        %td.body
          = procategory.name
= paginate @procategories