- if user_signed_in? && current_user.is_staff?
  = render partial: 'staff_show'
- else
  = render partial: 'customer_show'

= link_to 'Edit', edit_project_path(@project)
\|
= link_to 'Back', projects_path
