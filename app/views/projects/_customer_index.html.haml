.post
  %h2.title
    = t("views.welcome_to", item: 'Kho Điện Tử')
  %br
  %h3.posted Posted on January 1th, 2007 by Author
  .story
    %p
      %marquee{behavior: 'scroll', direction: 'left', scrollamount: 5}
        - (1..3).each do |item|
          - name = 'panel '+item.to_s+'.jpg'
          = image_tag(name, class: "company_panel")
    %p
      %em
        %strong Baron's Blog
      is a free template from
      %strong Free CSS Templates
      released under a
      = succeed "." do
        %a{:href => "http://creativecommons.org/licenses/by/2.5/"} Creative Commons Attribution 2.5 License
  .meta
    %p
      Filed under
      %a.category{:href => "http://www.free-css.com/"} Uncategorized
      |
      %a.comment{:href => "http://www.free-css.com/"} 1 Comment »
- @projects.each do |p|
  .post
    %h2.title
      = link_to p.title, p
    %h3.posted
      = format_date(p.created_at)
    .story
      %span
        .story_image
          - if p.photo.url.split('/').last == 'missing.png'
            = link_to image_tag("missing_panel.png"), p
          - else
            = link_to image_tag(p.photo.url(:thumb)), p
        .story_content
          = p.description.truncate(700)
    .meta
      %p
        = t("activerecord.models.procategory")
        = link_to truncate(p.procategory.name, length: 40), p.procategory